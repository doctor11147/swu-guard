# =============================================================================
# pip 依赖清单（Windows / Linux / macOS 通用）
#
# 用法：
#   conda create -n face python=3.10
#   conda activate face
#   pip install -r requirements.txt
#
# 注意：
#   - torch / torchvision 需根据你的 CUDA 版本选择，默认安装 CPU 版
#   - asyncmy 在 Windows 上可能需要 Rust 编译链
#   - faiss-cpu 在 Windows 上通过 conda-forge 安装更稳定（conda install faiss-cpu）
# =============================================================================

# ── 深度学习框架 ──────────────────────────────────────────
numpy>=1.26.0,<1.27.0
opencv-python>=4.10.0,<4.11.0
torch>=2.3.0
torchvision>=0.18.0
timm>=1.0.0
scikit-image>=0.24.0
Pillow>=10.0.0

# ── 核心推理 ─────────────────────────────────────────────
insightface==0.7.3
onnxruntime==1.18.1
faiss-cpu==1.8.0

# ── Web 框架 ────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6
websockets==12.0
watchfiles>=0.13
httptools>=0.6.3
# uvloop 仅 macOS/Linux 可用，Windows 需单独安装：
#   pip install uvloop  （Windows 上跳过，uvicorn 会自动回退到 asyncio）
python-multipart==0.0.9
pydantic>=2.9.0,<2.10.0
PyYAML==6.0.2
aiofiles==24.1.0

# ── 数据库 ──────────────────────────────────────────────
SQLAlchemy[asyncio]>=2.0.0,<2.1.0
asyncmy==0.2.10
alembic==1.13.3

# ── 认证 / 加密 ─────────────────────────────────────────
passlib[bcrypt]==1.7.4
bcrypt>=4.0,<4.1
python-jose[cryptography]==3.3.0

# ── 日志 / 工具 ─────────────────────────────────────────
loguru==0.7.2
python-dotenv==1.0.1

# ── 评估脚本用 ──────────────────────────────────────────
datasets==2.20.0

# ── 开发与测试（可选）────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
ruff==0.6.9
mypy==1.11.2
types-PyYAML
