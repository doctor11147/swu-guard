[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests
addopts = -ra --strict-markers
markers =
    slow: 加载推理 pipeline / 真实运行模型的端到端测试（CI 单独跑）
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
